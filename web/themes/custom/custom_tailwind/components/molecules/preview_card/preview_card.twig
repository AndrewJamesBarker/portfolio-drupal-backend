
<div {{ attributes.addClass('preview-card') }}>
  <div class="card-content">
    <h2 class="card-title">{{ title }}</h2>
    <p class="card-description">{{ description }}</p>
    {% if slots.cta %}
      <div class="card-button-wrapper">
        {{ slots.cta }}
      </div>
    {% endif %}
  </div>
</div>
